name: AstroBrief One-Time 2026-08-24 10:20 CST

on:
  schedule:
    # One-time test target: 2026-08-24 10:20 Beijing time = 02:20 UTC.
    # GitHub cron has no year/month/day field, so the job is limited to the first
    # scheduled run and the exact UTC date is verified before any mail is sent.
    - cron: '20 2 * * *'
  workflow_dispatch: {}

jobs:
  run-once:
    # For scheduled executions, only the first run of this workflow is eligible.
    # Manual dispatch remains available for debugging.
    if: ${{ github.event_name == 'workflow_dispatch' || github.run_number == 1 }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write

    steps:
      - name: Verify one-time target date
        if: ${{ github.event_name == 'schedule' }}
        run: |
          test "$(date -u +'%Y-%m-%d')" = "2026-08-24"

      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Restore semantic model cache
        uses: actions/cache@v5
        with:
          path: ~/.cache/huggingface
          key: astrobrief-models-${{ runner.os }}-v1
          restore-keys: |
            astrobrief-models-${{ runner.os }}-

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run AstroBrief one-time test
        env:
          TOKEN: ${{ github.token }}
          SMTP_HOST: smtp.gmail.com
          SMTP_PORT: '465'
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          SMTP_FROM: ${{ secrets.SMTP_FROM }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          ASTROBRIEF_CONTACT: ${{ vars.ASTROBRIEF_CONTACT }}
        run: python daily.py -t "${{ github.token }}"

      - name: Commit generated briefs and send state
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A -- LATEST.md briefs scores state 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "No generated AstroBrief changes to commit."
          else
            git commit -m "One-time AstroBrief test $(date -u +'%Y-%m-%dT%H:%MZ')"
            git push
          fi
